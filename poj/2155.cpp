#include <cstdio>
void modify(int x1, int y1, int x2, int y2) {
    int x = x1;
    while ()
}
void work() {
    scanf("%d%d", &n, &q);
    while (q--) {
        scanf("\n%c", ch);
        if (ch == 'C') {
            scanf("%d%d%d%d", &x1, &y1, &x2, &y2);
            modify(x1, y1, x2, y2);
        }

    }
}
int main() {
    scanf("%d", &T);
    while (T--) work();
    return 0;
}
