#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
int main() {
    scanf("%d%d%d", &n, &m, &d);
    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j <= m; ++j) {
            if (a[i][j]) continue;
        }
    }
    return 0;
}
